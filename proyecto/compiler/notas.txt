

La declaración de arreglos
es por línea, y solo se inicia con enteros

de que:

let miArray : int[10]
  Se declara un arreglo de enteros de 10 espacios
let datosA, datosB: int[8]
  Se declara un arreglo `datosA` y `datosB` de enteros de 8 espacios

---

Mi read no asigna

es que le pasas la variable



para media, mediana etc. habrá parche guadalupano


Para la varianza y STDEV
es de population no sample (osea es /n y no entre /n-1)