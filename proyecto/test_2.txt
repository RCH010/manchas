let valorNumerico:int;
valorNumerico = 12;

function funciona: bool(siFunciona:string) {
  if(siFunciona == "yes") {
    return true;
  } else {
    return false;
  }
}

main () {
  let strValue: string;
  read(strValue);
  if(funciona(strValue)) {
    print("Si funciona");
  } else {
    print("no, no funciona");
  }
}