program test3;
let valorNumerico, algo, mas:int;

function sumTwoNumbers: int(firstNumber: int, secondNumber: int) {
  return firstNumber + secondNumber;
}

function multiplyTwoNumbers: int(firstNumber: int, secondNumber: int)
let result: int;
{
  result = firstNumber * secondNumber;
  return result;
}

function divideTwoNumbers: float(firstNumber:float, secondNumber: float) {
  return (firstNumber / secondNumber);
}

function sumFrom0ToN: int(givenLimit: bool, smth: int, algo: int, sdad:float)
let i, result: int;
{
  for(i = 0 to givenLimit by 1) {
    print(i);
    result = result + i;
  }
  return result;
}

main ()
  let sumResult, multResult: int;
  let divResult: float;
  let aNumber: int;
{  
  aNumber = 12;
  sumResult = sumTwoNumbers();
  print("The result is: ", aNumber);
}